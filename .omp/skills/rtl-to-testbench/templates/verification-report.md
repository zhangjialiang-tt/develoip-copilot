# 验证报告 — <DUT 顶层模块名>

> 本文件汇总仿真结果与验证结论。字段固定，结论须有日志证据支撑。

## 元信息

| 项 | 值 |
| --- | --- |
| DUT 文件 | `<dut_file>` |
| 顶层模块 | `<top_module>` |
| 仿真器 | `<vsim|iverilog>` |
| 生成日期 | `<YYYY-MM-DD>` |
| 日志路径 | `<output_dir>/sim.log` |

## 编译结果

- 状态：<PASS / FAIL / 未执行>
- 编译器返回：<returncode 与关键错误行，或"仿真器不可用">
- 备注：<如缺源文件、参数未定义>

## 仿真结果

| 用例 ID | 场景 | 结果 | 关键证据 |
| --- | --- | --- | --- |
| TC1 | <smoke> | <PASS/FAIL> | <日志行/断言 ID> |
| TC2 | <典型> | <PASS/FAIL> | <…> |

- 总体：<X/Y 用例 PASS>
- self-checking 是否启用：<是/否>

## 失败定位（如有）

> 由 `simulation-analyst` 基于 `tools/simulation/extract_failures.py` 输出填写。

- 首个失败：<文件:行 / 信号 / 触发条件>
- 分类：<环境 / 工具 / 验证 / 设计>
- 下一步：<建议动作>

## 未确定项与风险

| 项 | 说明 | 影响 |
| --- | --- | --- |
| <如：协议假设未被覆盖> | <…> | <…> |

## 结论

- 是否达到 design Milestone3 验收：<是/部分/否>
- 是否需要用户补充行为定义：<是/否，列出>
- 后续建议：<回归/加固/补充用例>
