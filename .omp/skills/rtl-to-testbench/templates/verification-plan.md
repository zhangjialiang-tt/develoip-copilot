# 验证计划 — <DUT 顶层模块名>

> 本文件由 `verification-engineer` 依据 `tools/rtl/extract_interfaces.py`、`tools/rtl/scan_clock_reset.py` 的输出与用户说明生成。字段固定，禁止自由增删章节。

## 元信息

| 项 | 值 |
| --- | --- |
| DUT 文件 | `<dut_file>` |
| 顶层模块 | `<top_module>` |
| 仿真器 | `<vsim|iverilog>` |
| 生成日期 | `<YYYY-MM-DD>` |
| 输出目录 | `<output_dir>` |

## 验证目标

- 功能目标：<一句话描述 DUT 应实现的协议/算法行为>
- 不验证范围：<明确不在本计划内的项，例如时序/功耗/跨时钟域>

## 接口与时钟复位摘要

> 直接引用工具 `--json` 输出事实，不重述未知行为。

- 参数：<来自 extract_interfaces.params，或标注"无">
- 端口：<来自 extract_interfaces.ports，列出方向与位宽>
- 时钟：<来自 scan_clock_reset.clocks：信号/边沿/类型>
- 复位：<来自 scan_clock_reset.resets：信号/边沿/低有效/同步异步/断言表达式>

## 协议/行为假设

> 无工具可直接提取协议。下表每条结论须标注置信度（高/中/低）与依据。

| 行为点 | 假设 | 置信度 | 依据 |
| --- | --- | --- | --- |
| <如：有效握手> | <假设> | <高/中/低> | <工具证据/用户说明> |
| <如：突发长度> | <假设> | <高/中/低> | <工具证据/用户说明> |

- 未确定项：<列出无法推断、需用户补充的行为>

## 激励策略

- 激励类型：<定向/随机/定向+随机种子>
- 边界场景：<如：首拍、空满、背压、复位释放后首拍>
- 时钟/复位序列：<复位释放时刻、首有效时刻>

## Scoreboard 与断言

| 检查项 | 类型 | 说明 |
| --- | --- | --- |
| <如：输出数据匹配参考模型> | self-check | <比较方式> |
| <如：协议违例> | assertion | <触发条件> |

## 测试用例

| 用例 ID | 目标 | 输入特征 | 预期 |
| --- | --- | --- | --- |
| TC1 | <smoke> | <最小激励> | <编译通过+至少 1 检查 PASS> |
| TC2 | <典型场景> | <…> | <…> |

## 环境约定

- 仿真命令：`<如：vsim -c -do "run -all" work.<top>_tb>`
- 运行脚本：`<output_dir>/run_sim.<sh/do>`
- 日志路径：`<output_dir>/sim.log`
