# 2026-08-04

## rtl-architecture-analysis skill 优化分析（仅方案，未实施）

用户需求：如何优化该 skill。已读 SKILL.md、rtl-analyst agent、4 个 Python 工具、design.md §6.1、fixtures、smoke_test；并用 fixtures/rtl/normal/counter.v 实际跑通 4 个工具（输出正确）。

### 已确认问题（未闭环）
- **P0 一致性/确定性**：SKILL 与 design.md §6.1 冲突（design 列 7 产物含 dataflow.md，SKILL 只 5 个；Procedure 顺序不同）；When to use 声明"数据流/状态机/协议边界"但无工具支撑；templates/ 与 references/ 空目录（design 要求按需读取放模板）；产物输出目录未规定。
- **P1 工具正确性**：
  - extract_interfaces.py：宽度全局反查易误匹配；参数默认值 `([^,;)]+)` 遇 `$clog2(WIDTH)` 截断；非贪婪 `(.*?)` 遇参数默认值内 `)` 提前结束端口块。
  - build_hierarchy.py：实例化判定误判 function/task 调用、接口连接；不跨文件回填 module→file，多文件拼不出全局树。
  - scan_clock_reset.py：漏 always_ff、`if (rst==1'b0)`、`~rst`、内部生成时钟；不区分异步/同步、高/低有效。
- **P2 评估缺位**：evals/rtl-architecture-analysis 空；smoke_test 仅跑 --help 不验证真实输出；fixtures 仅 1 个 normal，缺 error/incomplete/real 三类。

### 建议实施顺序
1. P0 先落地报告模板（templates/ 5–7 份定死章节+表格）、补齐 dataflow.md、对齐 design §6.1、加输出目录约定（默认 .project/rtl-analysis/<top>/）。
2. P1 修三个工具 + 明确数据流/状态机/协议边界处置（加启发式工具或改为 agent 基于证据标注置信度，放 references/）。
3. P2 补四类 fixtures + smoke_test 真实断言 + evals 端到端样例。

### 约束提醒
- design.md 已 FROZEN v2.1：应让 SKILL 对齐 design，不要反向改 design（除非走 design-deviations）。
- 工具保持正则，不为未来需求引入通用 parser；仅当 fixture 证伪时才考虑 pyverilog/verilator（design 允许的偏差情形）。

## 待用户决策
- 用户选择"只给方案不动手"，实施待后续触发。

## P0 已实施（08:37 起）
按用户"实施p0"指令完成 P0（一致性 + 确定性）：
- 创建 6 个报告模板 `templates/`：rtl-analysis.md、interface-table.md、module-hierarchy.md、dataflow.md、clock-reset-map.md、risk-notes.md（定死章节+表格字段，引用工具 --json 输出）。
- SKILL.md 对齐 design §6.1：Outputs 补 `dataflow.md`（现 6 产物，与 design 一致）；Procedure 改为 7 步（先 rtl-analyst 规划→工具 --json→识别状态机/协议边界→语义解释→标记未确定→按模板生成）；Delegation/Safety 补充模板与默认输出目录 `.project/rtl-analysis/<top>/`。
- rtl-analyst.md 输出约定增加"按 templates/ 模板生成 6 个报告文件"。
- 验证：`tests/omp/smoke_test.py` 全 PASS（文件存在/CLI/结构审计）。
- 未做（属 P1）：工具正确性修复、数据流/状态机/协议边界专用工具或 references 启发式；属 P2：fixtures 四类补齐、smoke_test 真实断言、evals 端到端样例。

## 下一步建议
- P1：修 extract_interfaces 宽度/参数括号、build_hierarchy 误判+跨文件回填、scan_clock_reset 覆盖 always_ff/低有效/内部时钟。
- P2：补 fixtures 四类 + smoke_test 真实运行断言 + evals 样例。

## P1 已实施（08:42 起）
按用户"做 P1"完成工具正确性修复 + FSM/协议启发式，并用 fixture+测试保护：

### 工具修复（tools/rtl/）
- extract_interfaces.py：新增平衡括号解析 #(...) 与 (...) 头；端口宽度改为声明处就近解析（修复全局反查误匹配与 1-bit 误标 [0:0]）；参数默认值正则放宽允许括号（`$clog2(WIDTH)` 不再截断）。
- build_hierarchy.py：平衡括号解析模块头；实例化判定改为"先去掉 #(...) 再用 `MODNAME INST (`"；建立**跨文件 module→file 映射**回填子模块定义文件；非模块名/原语不误判。
- scan_clock_reset.py：平衡括号解析；覆盖 `always_ff`；复位支持低有效（`!rst`/`~rst`/`==1'b0`/名称`_n`）、同步/异步判定（敏感表 vs if）、断言表达式；内部生成时钟启发式（`<= ~clk`）；输出结构化 `clocks[{signal,edge,type}]` / `resets[{signal,edge,active_low,sync,assertion}]`。

### 启发式与模板
- 新增 `references/fsm-heuristics.md`：状态机/协议边界/数据流识别启发式 + 置信度标注规范（无专用工具，agent 基于证据人工判断）。
- 更新 `templates/clock-reset-map.md` 匹配新结构化输出（边沿/类型/有效性/同步异步/断言）。

### 测试保护
- 新增 fixtures：`fixtures/rtl/normal/param_vectored.sv`、`fixtures/rtl/hierarchy/{top,child}.sv`。
- 新增 `tests/omp/test_rtl_tools.py`：6 用例。`python tests/omp/test_rtl_tools.py` → 6 passed。
- `python tests/omp/smoke_test.py` → All smoke tests PASSED。

### 设计约束
- 工具仍为正则/平衡括号的"独立 Python CLI"，未引入第三方 parser/框架，未改 design.md（FROZEN）。

## 待办
- P2：补 fixtures 四类（error/incomplete/real）、smoke_test 真实运行断言、evals 端到端样例。

## P2 已实施（08:54 起）—— 评估闭环完成
按用户"继续下一步"完成评估闭环，对齐 docs/evaluation-plan.md 四类样例 + docs/design.md §1437：

### Fixtures 补齐（四类齐全）
- 新增 `fixtures/rtl/error/syntax_bad.v`（已知语法错误：模块头括号未闭合+缺 endmodule）。
- 新增 `fixtures/rtl/incomplete/truncated.v`（模块声明截断，缺 body/endmodule）。
- 新增 `fixtures/rtl/incomplete/multi_no_top.v`（3 平级模块 fifo_ctrl/arbiter/crc8，无单一顶层）。
- 新增 `real-samples/rtl/axi_stream_proc.v`（脱敏真实风格：AXI-Stream + 参数化 + 状态机 + 同文件子模块 sync_fifo + 生成使能）+ `real-samples/README.md`（记录格式/已知问题/状态，按 evaluation-plan）。

### 关键行为事实（已实测，供后续参考）
- 四个工具在 error/incomplete 上均 **rc=0、无 traceback、无 stderr**（尽力解析，不崩溃）。
- **行为不一致且可利用于完整性判定**：`extract_modules` 对截断/语法错误文件返回空 `[]`，而 `extract_interfaces` 仍宽松提取端口。已将"以 extract_modules 返回空为完整性权威信号"写入 SKILL.md Failure handling —— 对应 design 验收"缺少顶层时不盲目猜测""正确报告语法错误"的诚实边界（工具不编译，语法检查需编译器/仿真器/Agent）。
- 多模块无顶层时，`extract_modules`/`build_hierarchy` 均不臆造 top/root。

### 测试与验证
- 扩展 `tests/omp/test_rtl_tools.py`：新增 `TestErrorIncomplete`（3 用例：error 不崩溃/不伪造成功、truncated 返回空、multi 不臆造顶层）。现 9 passed。
- `smoke_test.py`：新增 `check_tool_real_output()`，对 normal/counter.v 实跑 4 工具并断言合法 JSON + 关键字段（原仅 --help）。现 All smoke tests PASSED（新增 "RTL tool real output" 项）。
- `real-samples/axi_stream_proc.v` 经 4 工具实测不崩溃，层级 u_fifo→sync_fifo 正确解析。

### evals 端到端
- 新增 `evals/rtl-architecture-analysis/skill-eval.md`：3 个 case（Normal / Error-Incomplete / Multi-no-top）+ rubric（预期包含/禁止出现/评分点）+ 结果表 TODO + 工具层配合验证说明。对齐 evaluation-plan §C。

### 设计约束遵守
- 工具未改（P2 仅补测试/样例/文档），不引入框架、不改 design.md（FROZEN）。
- SKILL.md 仅 Failure handling 段增强（完整性判定），不反向改 design。

## 当前状态总结
- rtl-architecture-analysis skill 优化 P0→P1→P2 全部完成。
- 四类 fixtures 齐全、6 个报告模板就位、3 个工具正确性修复、FSM 启发式、smoke_test 真实断言、evals 端到端 case 齐备。
- 验证：test_rtl_tools 9 passed；smoke_test All PASSED。
- 剩余开放项（非阻塞）：evals 结果表待端到端执行填写；语法错误自动检测需依赖外部编译器（已知边界，已文档化）。

## rtl-to-testbench skill 优化（09:19 起，用户"相同方式分析优化" + 选 P0+P1+P2 全做）
按首个 skill 同样框架分析并落地。关键差异：本 skill 无自有工具、模板/references/evals 均空；但引用的 4 个工具（extract_interfaces/scan_clock_reset/simulation/run/parse_log）全部真实存在，本机 vsim+iverilog 均可用 → 无"声明悬空工具"，P1 重心为"补能力引用+启发式+仿真器自适应"，不碰工具代码。

### P0 确定性 + design §6.2 对齐
- 新建 `templates/`：verification-plan.md、verification-report.md（两份报告定死章节/表格字段，引用工具 --json）、tb-top.sv.tmpl + run_sim.sh.tmpl（带占位符的脚手架）、vectors.md（向量说明）。
- SKILL.md 对齐 design §6.2：Procedure 统一为 10 步、顺序一致（design step2 是"rtl-analyst 提取接口/时钟/复位/协议"，协议为 agent 基于工具证据+用户说明推断并标置信度）；新增默认输出目录 `.project/tb/<top_module>/`；Tools 增补 extract_failures.py、aggregate_regression.py（此前被闲置）；Delegation 让 simulation-analyst 使用二者；Outputs 增加模板映射。
- 更新 agent：simulation-analyst.md、verification-engineer.md 输出约定指向 templates/ 与新增工具。

### P1 能力补齐
- 新增 `references/protocol-stimulus-heuristics.md`：协议/激励推断线索 + 置信度（高/中/低）标注规则 + 诚实边界（类比首个 skill 的 fsm-heuristics）。
- SKILL.md Validation/Failure handling 增加"仿真器自适应"：先检测 vsim/iverilog，缺省用可用者；均不可用则按 design Milestone3 验收"正确报告阻塞"，不伪造编译成功。
- 新增 DUT fixture `fixtures/rtl/normal/counter_buggy.v`（注入 cnt+2 错误，供 Milestone3 Case B 验证 self-check 可识别）；在 evals 登记 real-samples/rtl/axi_stream_proc.v 为真实 DUT 候选。

### P2 评估闭环
- 新增 `evals/rtl-to-testbench/skill-eval.md`：对齐 design Milestone3 三条 Fixture 验收（Case A 简单 DUT→可运行 self-checking tb / Case B 故意注入 DUT 错误→tb 识别 / Case C 仿真器不可用→正确报告阻塞）+ rubric + 结果表 TODO。
- `smoke_test.py` 新增 `check_simulation_tools_output()`：对 fixtures/simulation/{normal/sim_pass.log, error/sim_fail.log} 用 parse_log.py/extract_failures.py 断言合法 JSON + pass 字段。现 All smoke tests PASSED（新增 "Simulation tool real output" 项）。

### 验证
- `python tests/omp/smoke_test.py` → All smoke tests PASSED（5 项，含新增仿真工具断言）。
- DUT fixtures 经 extract_interfaces/scan_clock_reset 实测不崩溃，counter_buggy 的 clk/rst_n 正确识别（self-check 可捕获 +2 注入错误）。

### 设计约束遵守
- 未改 design.md（FROZEN）；未引入框架；未修改任何工具代码（本 skill 无自有工具，依赖工具已在首个 skill 的 P1 修好）。
- 仿真器自适应为诚实边界策略，非工具改动。

## simulation-analysis skill 优化（09:29 起，用户"相同方式分析优化" + 选 P0+P1+P2 全做）
按前两个 skill 同样框架分析并落地。**关键新发现：本 skill 存在真实的"声明悬空工具"** —— SKILL 引用 `tools/waveform/extract_window.py` 与 `tools/timing/parse_paths.py`，但两目录**均为空、工具未实现**。经查 design §10.3/§10.4，它们属 design 规划的 **Milestone 4（尚未实现）** 能力。处置方式类比首个 skill 的"语法错误自动检测"诚实边界：**SKILL 诚实标注为 Milestone 4 待实现，不伪造可用**，而非实现工具（超出 skill 优化范围）。另：产物清单已与 design §6.6 完全对齐（无缺口，无需补产物）；三个仿真工具真实可用，JSON 结构已实测；rtl-to-testbench P2 的 smoke 断言漏了 aggregate_regression，本 skill P2 补上。

### P0 确定性 + design §6.6 对齐 + 诚实标注悬空
- 新建 `templates/simulation-analysis.md`（主报告：证据/判断/建议/不确定性四类固定段 + 首个失败表 + 失败聚类表，字段固定）。
- 新建 `templates/artifacts-schema.md`（规定 first-failure.json / failure-clusters.json / signal-window.csv / timing-summary.md 字段契约；后两者标注"⚠️ Milestone 4 条件产物"）。
- SKILL.md 对齐 design §6.6：Procedure 补强"先证据后判断、不把末错当根因"；新增默认输出目录 `.project/sim-analysis/<case>/`；Tools 段把 waveform/timing 两工具标为"⚠️ Milestone 4 待实现（尚未落地）"并附诚实边界说明。
- simulation-analyst.md 输出约定改为优先使用自有 `templates/simulation-analysis.md`，失败定位细节可交叉引用 verification-report.md。

### P1 能力补齐（启发式 + 诚实边界）
- 新增 `references/failure-classification.md`：环境/工具/验证/设计四类问题判别线索 + 置信度（高/中/低）标注规则，类比前两个 skill 的 fsm/protocol 启发式；含"不把末错当根因""波形/时序证据不可用"边界。
- 波形/时序段落（When to use/Procedure）明确为 Milestone 4 待实现，工具落地后启用。

### P2 评估闭环
- 新增 `evals/simulation-analysis/skill-eval.md`：Case A 简单日志→定位首个失败+解释 / Case B 失败日志→区分环境/设计 / Case C 波形/时序输入→正确报告工具待实现（Milestone 4）+ rubric + 结果表 TODO（Agent 端到端，待真实跑填）。
- `smoke_test.py` 新增 `check_regression_aggregation()`：对 normal+error 两 log 用 aggregate_regression.py 断言合法 JSON + total=2/passed=1/failed=1 + details=2，并在 main() 注册。补上了 rtl-to-testbench P2 遗漏的聚合断言。

### 验证
- `python tests/omp/smoke_test.py` → All smoke tests PASSED（6 项，含新增 "Regression aggregation"）。
- 三个仿真工具（parse_log/extract_failures/aggregate_regression）真实 JSON 结构已实测确认，断言准确。

### 设计约束遵守
- 未改 design.md（FROZEN）；未引入框架；未实现波形/时序工具（属 Milestone 4 待办，超出 skill 优化范围）。
- 诚实标注悬空工具，不伪造 signal-window.csv / timing-summary.md。

### 三个 skill 优化共性结论（可复用）
- 统一手法：P0 模板定死章节/字段 + 对齐 design 的 Procedure/产物 + 默认输出目录 `.project/<skill>/<key>/`；P1 启发式 references（fsm / protocol-stimulus / failure-classification）标置信度；P2 evals 端到端 case + smoke_test 真实工具断言。
- 诚实边界是设计基线能力的"工具未就绪"时的统一应对：rtl-architecture-analysis 的语法检测、simulation-analysis 的波形/时序，均标注而非伪造。
- 不碰 FROZEN design.md、不引入框架、优先用独立 CLI + 正则/平衡括号。

